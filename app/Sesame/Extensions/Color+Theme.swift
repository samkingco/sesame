import SwiftUI

extension Color {
    /// Background behind sections / sheet chrome
    static let sesameGroupedBackground = Color(.systemGroupedBackground)

    /// Row/cell surface in forms
    static let sesameSurface = Color(.secondarySystemGroupedBackground)
}
